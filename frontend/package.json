{
  "name": "tournament-frontend",
  "version": "1.0.0",
  "description": "Flutter Frontend for Gaming Tournament",
  "scripts": {
    "dev": "flutter run -d web-server --web-port=3000 --web-hostname=0.0.0.0",
    "build": "flutter build web --no-web-resources-cdn --pwa-strategy=none",
    "start": "node server.js",
    "serve": "node server.js"
  }
}
